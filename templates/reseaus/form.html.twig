{{ form_row(formPost.category) }}
{{ form_row(formPost.content, {'attr': {'placeholder': "Exprimez vous"}}) }}
{{ form_row(formPost.img,
    {'attr': {'id':'inputGroupFile'}}) }}
{{ form_row(formPost.tags) }}


    {% for tag in formPost.tags %}
        {{ form_widget(tag.name , {'attr': {'class': 'dynchoice'}}) }}
    {% endfor %}

